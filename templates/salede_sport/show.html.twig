{% extends 'back.html.twig' %}

{% block title %}SaledeSport{% endblock %}

{% block body %}
    <h1>SaledeSport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salede_sport.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salede_sport.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salede_sport_index') }}">back to list</a>

    <a href="{{ path('salede_sport_edit', {'id': salede_sport.id}) }}">edit</a>

    {{ include('salede_sport/_delete_form.html.twig') }}
{% endblock %}
